# دليل استخدام الحقول الإجبارية - Required Fields Guide

## 📌 المكون الأساسي: `RequiredLabel`

تم إنشاء مكون `RequiredLabel` لعرض **نجمة حمراء (*)** بجانب جميع الحقول الإجبارية في الموقع.

### 📁 موقع المكون
```
src/components/RequiredLabel.tsx
```

---

## 🎯 كيفية الاستخدام

### 1. استيراد المكون

```tsx
import RequiredLabel from '@/components/RequiredLabel';
```

### 2. الاستخدام الأساسي

#### قبل (الطريقة القديمة):
```tsx
<div>
  <label className="block text-sm font-medium text-gray-700 mb-2">
    الاسم الكامل *
  </label>
  <input 
    type="text" 
    className="w-full px-4 py-2 border border-gray-300 rounded-lg"
  />
</div>
```

#### بعد (الطريقة الجديدة):
```tsx
<div>
  <RequiredLabel>الاسم الكامل</RequiredLabel>
  <input 
    type="text" 
    className="w-full px-4 py-2 border border-red-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500"
    required
  />
</div>
```

---

## 🎨 الأنماط الموصى بها

### للحقول الإجبارية:

```tsx
className="w-full px-4 py-2 border border-red-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500"
```

### للحقول الاختيارية:

```tsx
className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500"
```

---

## 📦 خصائص المكون (Props)

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `children` | `ReactNode` | - | محتوى Label (النص) |
| `required` | `boolean` | `true` | عرض النجمة الحمراء |
| `className` | `string` | `''` | CSS classes إضافية |
| `htmlFor` | `string` | - | ربط Label بـ input |

---

## 💡 أمثلة متقدمة

### مثال 1: Label مع htmlFor

```tsx
<RequiredLabel htmlFor="email">البريد الإلكتروني</RequiredLabel>
<input 
  id="email"
  type="email" 
  className="w-full px-4 py-2 border border-red-300 rounded-lg"
  required
/>
```

### مثال 2: حقل اختياري (بدون نجمة)

```tsx
<RequiredLabel required={false}>ملاحظات إضافية</RequiredLabel>
<textarea 
  className="w-full px-4 py-2 border border-gray-300 rounded-lg"
/>
```

### مثال 3: تخصيص الأنماط

```tsx
<RequiredLabel className="text-lg font-bold">
  العنوان الرئيسي
</RequiredLabel>
```

---

## 🌟 مكون النجمة المستقل: `RequiredMark`

إذا كنت تحتاج فقط لعرض النجمة بدون Label:

```tsx
import { RequiredMark } from '@/components/RequiredLabel';

<span>
  الحقل المطلوب <RequiredMark />
</span>
```

---

## 📋 قائمة التحقق للمطورين

عند إضافة حقل جديد إجباري:

- [ ] استخدم `<RequiredLabel>` بدلاً من `<label>`
- [ ] أضف `border-red-300` للحقل
- [ ] أضف `focus:ring-red-500` و `focus:border-red-500`
- [ ] أضف `required` attribute للـ input
- [ ] تحقق من وجود validation مناسب

---

## 🎯 الحقول الإجبارية الحالية

### صفحة البيانات الإضافية (`/properties/[id]/additional`)

#### 1. بيانات المالك:
- ✅ الاسم الكامل
- ✅ رقم البطاقة المدنية
- ✅ تاريخ انتهاء البطاقة
- ✅ رقم الهاتف
- ✅ البريد الإلكتروني

#### 2. بيانات الموظفين:
- ✅ اسم مسؤول الصيانة
- ✅ رقم هاتف مسؤول الصيانة

#### 3. بيانات العقار:
- ✅ رقم المبنى
- ✅ نوع استعمال الأرض
- ✅ المنطقة
- ✅ رقم الرسم المساحي
- ✅ رقم القطعة

#### 4. حسابات الخدمات (كهرباء/مياه):
- ✅ رقم الحساب
- ✅ صورة العداد
- ✅ نوع الدفع (مسبق/آجل)

#### 5. المستندات:
- ✅ نسخة من الملكية
- ✅ الرسم المساحي

---

## 🔧 للمطورين المستقبليين

### كيف تطبق المكون على صفحة جديدة؟

1. افتح الصفحة التي تريد تحديثها
2. استورد المكون:
   ```tsx
   import RequiredLabel from '@/components/RequiredLabel';
   ```
3. ابحث عن جميع الـ `<label>` التي تحتوي على `*`
4. استبدلها بـ `<RequiredLabel>`
5. حدّث CSS classes للـ input ليكون بحدود حمراء
6. أضف `required` attribute

---

## 🎨 الألوان المستخدمة

```css
/* النجمة الحمراء */
text-red-600

/* حدود الحقل */
border-red-300

/* Focus ring */
focus:ring-red-500
focus:border-red-500
```

---

## 📱 تطبيق على الموقع كاملاً

تم تطبيق المكون على:
- [x] `/properties/[id]/additional` - البيانات الإضافية

قيد التطبيق:
- [ ] `/tenants/add` - إضافة مستأجر
- [ ] `/owners/add` - إضافة مالك
- [ ] `/properties/new` - إضافة عقار
- [ ] `/rentals/new` - إنشاء عقد

---

## 💡 نصائح إضافية

1. **الاتساق**: استخدم المكون في **جميع** الصفحات للاتساق
2. **الوضوح**: النجمة الحمراء توضح للمستخدم الحقول الإجبارية فوراً
3. **التحقق**: تأكد من وجود validation code يتحقق من هذه الحقول
4. **الترجمة**: النجمة (*) مفهومة عالمياً ولا تحتاج ترجمة

---

## 📞 الدعم

إذا كان لديك أي أسئلة أو ملاحظات، راجع:
- `src/components/RequiredLabel.tsx` - كود المكون
- `src/pages/properties/[id]/additional.tsx` - مثال كامل للتطبيق

---

**آخر تحديث**: 9 نوفمبر 2025  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للاستخدام

