# انسخ هذا الملف إلى .env و .env.local واملأ القيم محليًا
DATABASE_URL="file:./prisma/dev.db"

NEXT_PUBLIC_DEFAULT_LOCALE=ar
NEXT_PUBLIC_SUPPORTED_LOCALES=ar,en,fr,hi,fa,ur
NEXT_PUBLIC_CURRENCIES=OMR,AED,SAR,QAR,BHD,KWD,USD
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

NEXT_PUBLIC_ENABLE_GUARD=false

TRANSLATE_API_KEY=
NOTIFY_PROVIDER_KEY=

# مفتاح المدير لحماية لوحات الإدارة وواجهات الكتابة
ADMIN_KEY=changeme-StrongKey-2025

# قاعدة عنوان خدمة الترقيم (عند التطوير اتركها كما هي)
SEQ_BASE_URL=http://localhost:3000

# نسبة عربون للحجز عند البيع (إن رغبت استخدام فاتورة للعربون)
SALE_DEPOSIT_PERCENT=5

# ——— الصلاحيات ———
ADMIN_KEY=changeme-StrongKey-2025

# ——— خدمة الترقيم (تُستخدم داخليًا عند إصدار فواتير) ———
SEQ_BASE_URL=http://localhost:3000

# ——— الإعدادات المالية ———
SALE_DEPOSIT_PERCENT=5           # عربون البيع %
SERVICE_FEE_PERCENT=2.5          # رسوم خدمة افتراضية %
COUPON_DEFAULT_PERCENT=0         # خصم افتراضي إذا مرّ كود "DEFAULT" (اختياري)

# ——— البريد (SMTP) ———
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false                # true لTLS 465
SMTP_USER=no-reply@example.com
SMTP_PASS=your_smtp_password
SMTP_FROM="Ain Oman <no-reply@example.com>"

# ——— واتساب (Meta Cloud API) ———
WHATSAPP_TOKEN=EAAG...           # توكن الوصول
WHATSAPP_PHONE_ID=123456789012345
WHATSAPP_DEFAULT_TO=9689XXXXXXXX # (اختياري) رقم افتراضي E.164 لاختبار الإرسال
