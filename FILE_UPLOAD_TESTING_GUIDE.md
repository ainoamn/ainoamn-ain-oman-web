# 📋 دليل اختبار رفع الملفات

## 🎯 الهدف
التأكد من أن أزرار رفع الملفات في صفحة `/properties/[id]/additional` تعمل بشكل صحيح.

---

## 🔧 التقنية المُستخدمة

تم استخدام **useRef + onClick** لتشغيل نافذة اختيار الملف برمجياً:

```typescript
// إنشاء المرجع
const nationalIdFileRef = useRef<HTMLInputElement>(null);

// Input مخفي
<input ref={nationalIdFileRef} type="file" className="hidden" />

// زر يفتح Input
<button onClick={() => nationalIdFileRef.current?.click()}>
  اضغط لاختيار الملف
</button>
```

---

## ✅ خطوات الاختبار

### 1️⃣ **تحضير البيئة**
```bash
# تأكد من أن السيرفر يعمل
npm run dev
```

### 2️⃣ **فتح الصفحة**
افتح المتصفح على:
```
http://localhost:3000/properties/P-20251022094422/additional
```

### 3️⃣ **فتح Developer Console**
- اضغط **F12** أو **Ctrl+Shift+I**
- اذهب إلى tab **"Console"**

### 4️⃣ **إعادة تحميل كاملة**
- اذهب إلى tab **"Network"**
- ✅ ضع علامة على **"Disable Cache"**
- اضغط **Ctrl+Shift+R** (Windows) أو **Cmd+Shift+R** (Mac)
- انتظر حتى تُحمّل الصفحة تماماً

### 5️⃣ **اختبار الأزرار**

#### أ) **بيانات المالك** - زر رفع البطاقة الشخصية
1. مرّر إلى قسم "بيانات المالك"
2. ابحث عن "نسخة من البطاقة الشخصية *"
3. اضغط على الزر الأحمر الكبير: **"اضغط لاختيار ملف البطاقة"**

**المتوقع:**
- تفتح نافذة اختيار الملف فوراً
- في Console تظهر: `📎 تم رفع ملف البطاقة: filename.jpg ...`

#### ب) **حسابات الخدمات** - زر رفع صورة العداد
1. مرّر إلى قسم "أرقام حسابات الخدمات"
2. اضغط **"إضافة حساب خدمة جديد"**
3. اختر **"كهرباء"** أو **"مياه"**
4. اضغط على الزر: **"اضغط لاختيار صورة العداد"**

**المتوقع:**
- في Console تظهر: `🖱️ تم الضغط على زر رفع صورة العداد`
- تفتح نافذة اختيار الملف
- بعد اختيار ملف تظهر: `📸 تم اختيار ملف: meter.jpg ...`

#### ج) **المستندات** - زر رفع المستند
1. مرّر إلى قسم "المستندات المرفقة"
2. اضغط **"إضافة مستند جديد"**
3. اضغط على الزر الأحمر الكبير: **"اضغط لاختيار الملف"**

**المتوقع:**
- في Console تظهر: `🖱️ تم الضغط على زر رفع المستند`
- تفتح نافذة اختيار الملف
- بعد اختيار ملف تظهر: `📄 تم رفع مستند: document.pdf ...`

---

## 🐛 إذا لم تعمل الأزرار

### السيناريو 1: لا يحدث شيء عند النقر
**الأسباب المحتملة:**
- التغييرات لم يتم تحميلها
- Cache المتصفح قديم
- خطأ JavaScript في الصفحة

**الحلول:**
1. **Hard Refresh**: `Ctrl+Shift+R` عدة مرات
2. **Clear Cache Manual Clean**:
   - `F12` → `Application` → `Clear Storage` → `Clear site data`
3. **إعادة تشغيل السيرفر**:
   ```bash
   # أوقف السيرفر (Ctrl+C)
   npm run dev
   ```
4. **تأكد من عدم وجود أخطاء في Console**

### السيناريو 2: خطأ في Console
**ما يجب فعله:**
1. انسخ الخطأ بالكامل
2. أرسل الخطأ للمطور

### السيناريو 3: تفتح نافذة الملف ولكن لا يُحفظ الملف
**السبب:**
- هذا طبيعي حالياً! نحن فقط نختبر أن نافذة اختيار الملف تفتح
- رفع الملفات الفعلي يتطلب إعداد API endpoint منفصل

---

## 📊 قائمة التحقق النهائية

- [ ] السيرفر يعمل على `http://localhost:3000`
- [ ] الصفحة تُحمّل بدون أخطاء
- [ ] Developer Console مفتوح
- [ ] Cache معطل (`Disable Cache` ✅)
- [ ] تم عمل Hard Refresh
- [ ] **زر رفع البطاقة** - يفتح نافذة الملف ✅
- [ ] **زر رفع صورة العداد** - يفتح نافذة الملف ✅
- [ ] **زر رفع المستند** - يفتح نافذة الملف ✅
- [ ] **زر رفع العداد القديم** (في استبدال العداد) - يفتح نافذة الملف ✅
- [ ] **زر رفع العداد الجديد** (في استبدال العداد) - يفتح نافذة الملف ✅

---

## 🎨 التصميم المتوقع

### شكل الزر قبل اختيار ملف:
```
┌──────────────────────────────────────────────┐
│                                              │
│    📤  اضغط لاختيار الملف                  │
│                                              │
└──────────────────────────────────────────────┘
```
- خلفية حمراء شفافة
- حدود حمراء
- أيقونة رفع 📤
- cursor: pointer

### بعد اختيار ملف:
```
┌──────────────────────────────────────────────┐
│  ✅  filename.jpg  (245.67 KB)               │
└──────────────────────────────────────────────┘
```
- خلفية خضراء
- علامة صح ✓
- اسم الملف + الحجم

---

## 📞 دعم

إذا واجهت أي مشكلة:
1. تأكد من اتباع جميع الخطوات أعلاه
2. أرسل screenshot من Console
3. أرسل screenshot من الزر
4. صف المشكلة بالتفصيل

---

## ✅ الحالة الحالية

**آخر تحديث:** 9 نوفمبر 2025  
**Commit:** `420f500`  
**الملف:** `src/pages/properties/[id]/additional.tsx`

### التغييرات المُطبقة:
- ✅ إضافة `useRef` من React
- ✅ إنشاء 5 refs للـ file inputs
- ✅ تغيير `<label>` إلى `<button>` مع `onClick`
- ✅ إضافة `console.log` للتتبع
- ✅ تطبيق `type="button"` لمنع Form Submission

---

**مُلاحظة هامة:** إذا لم تعمل الأزرار حتى بعد اتباع كل الخطوات، قد تكون هناك مشكلة في البيئة. جرّب فتح الصفحة في متصفح آخر (Chrome, Firefox, Edge).

