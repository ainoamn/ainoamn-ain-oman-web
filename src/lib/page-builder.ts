export class PageBuilder {
  static addPage(pageConfig: {
    path: string;
    component: React.ComponentType;
    menuTitle: string;
    requiredPermission?: string;
  }) {
    // إضافة الصفحة إلى نظام التوجيه
    // إضافة عنصر إلى القائمة الجانبية
    // تحديث نظام الصلاحيات
  }
}